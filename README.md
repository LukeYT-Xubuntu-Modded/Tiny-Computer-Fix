# Tiny-Computer-Fix
Error when install some package or update? Fix it in one click

```bash
wget https://github.com/LukeYT-Xubuntu-Modded/Tiny-Computer-Fix/blob/main/sources.list && rm /etc/apt/sources.list && mv sources.list /etc/apt/
```
